import React from 'react'

const about = (props) => {
    return (
        <div>
            TEXT = {props.values.text}
            PASSWORD = {props.values.password}
        </div>
    )
}

export default about
